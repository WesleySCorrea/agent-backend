spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/my-agent-postgres
    username: agent
    password: agent123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    template:
      reply-timeout: 5000

app:
  queue: agent.commands
  defaultPath: "C:\\Users\\Public"

server:
  port: 8080
